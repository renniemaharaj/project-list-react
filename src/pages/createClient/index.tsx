import { ClientForm } from "../../pkg/forms/ClientForm";

const CreateProject = () => {
  return (
    <div className="pb-25">
      <ClientForm onClose={() => {}} />
    </div>
  );
};

export default CreateProject;
