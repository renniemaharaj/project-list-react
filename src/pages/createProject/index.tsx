import { ProjectForm } from "../../pkg/forms/ProjectForm";

const CreateProject = () => {
  return (
    <div className="pb-25">
      <ProjectForm onClose={() => {}} />
    </div>
  );
};

export default CreateProject;
